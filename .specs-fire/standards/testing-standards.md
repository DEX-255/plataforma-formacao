# Testing Standards

> **Ponteiro.** A fonte de verdade é
> [`specs/07-qualidade.md`](../../specs/07-qualidade.md), seção *O que testar*.
> Divergindo, `specs/07` vence.

## A ordem de importância

**1. As regras invariantes.** Uma suíte com um teste por `RN-xx`, referenciando o
código da regra. **É a única parte em que teste não é opcional** — cada uma dessas
regras existe porque sua violação causa dano real a uma pessoa.

**2. Autorização, nas duas camadas.** Pela aplicação: participante tentando abrir rota
de mentor, perfil de outro participante, feedback de encontro não liberado. E **direto
contra a API**, com um token de participante real, sem passar pela interface: pedir a
tabela `feedback` crua, pedir dado de outro participante. O esperado é recusa ou
resultado vazio nos dois casos.

> Testar só pela interface não prova que a política está certa. Um teste que passa
> porque o botão está escondido não testou nada.

**3. Anonimato.** Tentar reidentificar o autor de uma mensagem **tendo acesso total ao
banco**. Se for possível, é defeito de severidade máxima.

**4. Fluxo do mentor ponta a ponta.** Criar encontro → atribuir eixos → abrir →
registrar feedback → liberar → participante vê. É o caminho que precisa funcionar no
primeiro dia.

**5. Ciclo de vida.** Transições válidas e inválidas de encontro e de edição.
Especialmente: `liberado` não volta para `aberto`, e edição encerrada derruba acesso de
participante.

## Definição de pronto para um work item

- Os `RN` que ele toca têm teste, e o teste cita a regra.
- Os critérios de aceite do work item viraram asserção, não inspeção visual.
- Nenhum dado de bloco interno aparece na resposta do servidor para papel
  `participante` — verificado, não presumido.

## Testes manuais, antes do primeiro uso real

Não são substituíveis por automação:

- Percorrer a tela de registrar feedback **num celular de verdade**, em pé, com uma mão,
  cronometrando. **Se um registro leva mais de um minuto, a tela precisa mudar antes de
  setembro.**
- Ler um documento final **impresso, em papel**.

## Orçamentos que também são testes

Medidos em 4G simulado, na tela de registrar feedback:

| | Meta |
|---|---|
| Primeiro conteúdo | < 1,5s |
| Interativa | < 2,5s |
| Salvar feedback | < 1s, com confirmação otimista |
| JavaScript da rota | < 120 KB comprimido |

## Acessibilidade

Verificar em toda tela entregue: contraste conforme a *regra do roxo*
(`specs/05-design-system.md`), alvo de toque ≥ 44×44px, fluxo de feedback operável só
com teclado, anel de foco preservado, `prefers-reduced-motion` respeitado, `lang="pt-BR"`.

---
*Generated by specs.md - fabriqa.ai FIRE Flow*
